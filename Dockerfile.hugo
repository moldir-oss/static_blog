# Hugo Extended Docker Image
# Dependabot watches this file (docker ecosystem)
# When a new Hugo version drops, Dependabot opens a PR
# to bump this tag → the auto-merge workflow propagates the version
FROM klakegg/hugo:0.165.0-ext-ubuntu AS builder