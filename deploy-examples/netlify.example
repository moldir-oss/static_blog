# === Netlify example — local config, DO NOT COMMIT with your values ===
# Usage:
#   cp deploy-examples/netlify.example .deploy.local   (then fill in below)
#   npm run package                                     (build + ZIP)
#   npm run package:deploy                              (build + ZIP + auto-upload, tokens required)
#
# Notes:
# - Normal deploy: import the repo on app.netlify.com (netlify.toml
#   already runs the build + IP functions).
# - Manual ZIP upload: dashboard > site > Deploys > drag and drop the
#   public/ folder (unzip first).
# - IP counter: single Worker+D1 backend recommended (see IPS.md) —
#   fill in HUGO_VISIT_API_URL below. Without it, deprecated native Blobs
#   fallback (no TTL — uneven retention).

# Target platform (do not change in this file)
TARGET=netlify

# Final public URL — REQUIRED:
# https://mon-site.netlify.app  |  https://www.mon-domaine.fr
SITE_URL=https://mon-site.netlify.app

# --- Single visits/IP backend (optional, see IPS.md) ---
# HUGO_VISIT_API_URL=https://mon-licenciement-visits.<account>.workers.dev/api/visit

# --- SEO: verification + IndexNow (optional, see DEPLOY.md SEO section) ---
# HUGO_GOOGLE_SITE_VERIFICATION=
# HUGO_BING_SITE_VERIFICATION=
# INDEXNOW_KEY= (generate via npm run seo:init)

# --- Giscus comments (optional, see COMMENTAIRES.md) ---
# Uncomment and fill in the 4 values copied from https://giscus.app
# GISCUS_REPO=USER/REPO
# GISCUS_REPO_ID=R_xxx
# GISCUS_CATEGORY=Announcements
# GISCUS_CATEGORY_ID=DIC_xxx

# --- API tokens (required ONLY for `npm run package:deploy`) ---
# User Settings > Applications > Personal access tokens ; Site ID: Site settings > General
# NETLIFY_AUTH_TOKEN=
# NETLIFY_SITE_ID=
